# Receipt Upload Configuration
# This uses the same Lambda endpoint as applications, just with /presign path
# Format: https://YOUR_API_ID.execute-api.REGION.amazonaws.com/STAGE/presign
# Example: https://s8uentbcpd.execute-api.ap-southeast-1.amazonaws.com/dev/presign
VITE_RECEIPT_PRESIGN_URL=

# Instructions:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Set the URL to your main Lambda endpoint + /presign
#    Same base URL as the applications endpoint, just change /applications to /presign
# 3. Set up S3 bucket and configure S3_BUCKET_NAME in Lambda environment
# 4. Restart your dev server (npm run dev)
